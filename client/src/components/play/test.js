return (
  << div {...props } > Komponente { props.children }</div > {...this.props}>
    <Arrow
      style={style}
      onClick={this.toggleOpen}
      onContextMenu={this.close}
    >
      >
            </Arrow>
    {this.props.children}
  </<div {...props} >Komponente {props.children}</div>>
);
